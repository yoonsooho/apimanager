// import { JsonValue } from "@prisma/client/runtime/library";

export type rowElementType = {
    status: string;
    method: string;
    endPoint: string;
    queryString: string;
    request: string;
    response: string;
    memo: string;
    updatedAt: Date;
    createdAt: Date;
    // userId: number;
    id: number;
};
